# API Configuration
api:
  replicas: 2
  image:
    repository: ghcr.io/carlosfcruz/devopsproject
    tag: d5e604995ccb3f08a8d6e118b51c8e6a225fd8f1
  port: 8000
  env:
    APP_NAME: "platform"
    APP_ENV: "staging"
    LOG_LEVEL: "info"
    POSTGRES_HOST: "postgres"
    REDIS_HOST: "redis"

# PostgreSQL Configuration
postgres:
  image: postgres:16
  port: 5432
  storage: 1Gi

# Redis Configuration
redis:
  image: redis:7
  port: 6379

# Ingress Configuration
ingress:
  enabled: true
  host: platform.local
  className: nginx

# Secret reference
secret:
  name: db-credentials
