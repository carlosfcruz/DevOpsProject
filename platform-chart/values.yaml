# API Configuration
api:
  replicas: 2
  image:
    repository: ghcr.io/carlosfcruz/devopsproject
    tag: 08b281432cd5ca2e4d38a1eeae10d1d6bc56ad46
  port: 8000
  env:
    APP_NAME: "platform"
    APP_ENV: "staging"
    LOG_LEVEL: "info"
    POSTGRES_HOST: "postgres"
    REDIS_HOST: "redis"

# PostgreSQL Configuration
postgres:
  image: postgres:16
  port: 5432
  storage: 1Gi

# Redis Configuration
redis:
  image: redis:7
  port: 6379

# Ingress Configuration
ingress:
  enabled: true
  host: platform.local
  className: nginx

# Secret reference
secret:
  name: db-credentials
