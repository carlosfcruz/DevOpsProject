# API Configuration
api:
  replicas: 2
  image:
    repository: ghcr.io/carlosfcruz/devopsproject
    tag: 0dc5f88e44a0ac768bbb32b19534f2a76d1c8e80
  port: 8000
  env:
    APP_NAME: "platform"
    APP_ENV: "staging"
    LOG_LEVEL: "info"
    POSTGRES_HOST: "postgres"
    REDIS_HOST: "redis"

# PostgreSQL Configuration
postgres:
  image: postgres:16
  port: 5432
  storage: 1Gi

# Redis Configuration
redis:
  image: redis:7
  port: 6379

# Ingress Configuration
ingress:
  enabled: true
  host: platform.local
  className: nginx

# Secret reference
secret:
  name: db-credentials
